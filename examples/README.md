# examples/

Runnable, self-contained examples: small strategies plus the sample data manifests they
require. Each example doubles as living documentation of the contracts and as a smoke test.

Examples use only tiny **synthetic** data from `fixtures/` — never real or proprietary feeds.
