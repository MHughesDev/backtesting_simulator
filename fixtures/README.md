# fixtures/

Tiny, **synthetic** test data only. Hand-crafted or generated samples that exercise the
contracts and engines deterministically.

**The simulator owns no market data.** Nothing real, proprietary, or large belongs here — those
are the caller's responsibility. If a fixture starts to look like a dataset, it's in the wrong
place.
