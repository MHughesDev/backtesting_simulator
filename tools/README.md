# tools/

Developer tooling and scripts: code generation, schema/contract exporters (e.g. emitting
JSON Schema or `.pyi` stubs from the Rust contracts), fixture generators, and local dev
helpers.

Not part of the shipped library.
